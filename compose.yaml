services:
  postgres:
    image: 'postgres:16-alpine'
    environment:
      - 'POSTGRES_DB=tourdegjug'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=tourdegjug'
    ports:
      - '5432'
